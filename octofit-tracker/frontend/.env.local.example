# Define VITE_CODESPACE_NAME to enable Codespaces API base URL
# Example:
# VITE_CODESPACE_NAME=your-codespace-name

# When set, frontend will hit:
# https://${VITE_CODESPACE_NAME}-8000.app.github.dev/api/<route>/

# When unset, frontend falls back to:
# http://localhost:8000/api/<route>/
